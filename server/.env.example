# Server
PORT=4000

# DB primary preference: local | cloud
MONGO_PRIMARY=local

# Local MongoDB Community (default)
MONGODB_URI_LOCAL=mongodb://127.0.0.1:27017/cotonou

# Fallback (e.g., MongoDB Atlas) — leave empty if none
MONGODB_URI_FALLBACK=

# Retry tuning (optional)
DB_MAX_RETRIES=3
DB_RETRY_BASE_DELAY_MS=500
DB_SERVER_SELECTION_TIMEOUT_MS=5000
DB_MAX_POOL_SIZE=10
